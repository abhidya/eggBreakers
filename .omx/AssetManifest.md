# Asset Manifest

Source of truth: `src/ReplicatedStorage/Shared/AssetManifest.lua`.
G011/G013 rule: unique Creator Store `SourceAssetId` values are tracked separately from local manifest IDs; cloned duplicates do not count.
