# Security Threat Model

Server rejects client authority over currency, growth, combat distance, safe-zone attacks, remote spam, food diet/distance/depletion, and imported scripts. Remote rate limits are defined in `RemoteContracts` and enforced by server services.
